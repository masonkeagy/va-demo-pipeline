# Solution Source Contents (Placeholder)

This folder represents the **unpacked, source-controlled contents** of the
`CustomerServiceDemo` Dataverse solution, as produced by:

```bash
pac solution unpack --zipfile CustomerServiceDemo.zip --folder ./src